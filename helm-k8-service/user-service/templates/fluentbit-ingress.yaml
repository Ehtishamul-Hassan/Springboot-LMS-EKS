apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fluentbit-ingress
  namespace: elk
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
    - http:
        paths:
          - path: /fluentbit
            pathType: Prefix
            backend:
              service:
                name: fluentbit
                port:
                  number: 2020
